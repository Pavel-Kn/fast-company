import React from "react";
import UsersList from "../components/usersList";

const Users = () => {
    return (
        <div>
            <UsersList />
        </div>
    );
};

export default Users;
